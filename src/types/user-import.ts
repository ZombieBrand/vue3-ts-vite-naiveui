export interface TResults {
    姓名: string;
    开通时间: string;
    联系方式: string;
    角色: string;
}

export interface TUserInfo {
    username: string
    mobile: string
    role: string
    openTime: string
}